



leftRotate = (a, d) =>{
    //make forloop to iterate through array
    for(i = 0; i <= d; i++){}
    //
    //create a temp for each loop swap number with
    //
}

arr = [1,2,3,4,5]
num = 4

leftRotate(arr, num)